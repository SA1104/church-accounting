/**
 * Booza Think Platform OS - Media Engine Template Transformer Stub
 */

function transformTemplate(template, data) {
  console.log(`[Media Transformer] Interpolating variables into template...`);
  return `Parsed Content with values: ${JSON.stringify(data)}`;
}

module.exports = {
  transformTemplate
};

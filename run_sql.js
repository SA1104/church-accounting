const { Pool } = require('pg'); const fs = require('fs'); require('dotenv').config({path: 'backend/.env.development'}); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); async function migrate() { try { const file = process.argv[2]; const sql = fs.readFileSync(file, 'utf8'); await pool.query(sql); console.log('SQL successful:', file); } catch(e) { console.error('SQL failed', e); } finally { pool.end(); } } migrate();
